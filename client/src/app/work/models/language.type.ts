export type WorkLanguage = {
  [key: string]: {
    contentProduction: {
      title: { first: string; second: string };
      first: string;
      second: string;
    };
    promotionalVideo: {
      title: { first: string; second: string };
      first: string;
      second: string;
    };
    event: {
      title: { first: string; second: string };
      first: string;
      second: string;
    };
    solution: {
      title: { first: string; second: string };
      first: string;
      second: string;
    };
  };
};
